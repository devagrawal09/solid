diff --git a/webdriver-ts/src/puppeteerAccess.ts b/webdriver-ts/src/puppeteerAccess.ts
index 8ab2891..4abeb9b 100644
--- a/webdriver-ts/src/puppeteerAccess.ts
+++ b/webdriver-ts/src/puppeteerAccess.ts
@@ -118,6 +118,7 @@ export async function startBrowser(benchmarkOptions: BenchmarkOptions): Promise<
   const args = [
     `--window-size=${window_width},${window_height}`,
     "--js-flags=--expose-gc",     // needed for gc() function
+    ...(process.env.JFB_NO_SANDBOX ? ["--no-sandbox"] : []), // container runs as root
     "--no-default-browser-check", 
     "--disable-sync",           
     "--no-first-run",     
